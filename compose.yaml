services:
  lexforum:
    build: .
    ports:
      - "127.0.0.1:8000:8000"
    env_file: .env
    environment:
      DATA_DIR: /app/data
    volumes:
      - lexforum-data:/app/data
    restart: unless-stopped
volumes:
  lexforum-data:
