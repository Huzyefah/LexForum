# Server only: never put API keys in VITE_ variables.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-3.6-flash
DATA_DIR=backend/app/data
# Optional bearer token required for API access when set.
APP_ACCESS_TOKEN=

